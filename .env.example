# Copy to .env and fill in. .env is gitignored — nothing here reaches the repo.

# --- TigerGraph Savanna (https://savanna.tgcloud.io) ---
# Host is the workspace's connection URL, e.g. https://<subdomain>.i.tgcloud.io
TG_HOST=https://CHANGEME.i.tgcloud.io
TG_GRAPH=FraudInvestigation
TG_USERNAME=tigergraph
TG_PASSWORD=
# Optional: a TigerGraph secret/token if you generated one (else leave blank).
TG_SECRET=

# --- Google Gemini (https://aistudio.google.com/apikey — free tier) ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# --- Dataset (external; not committed) ---
DATASET_DIR=C:/Users/Smruti/Downloads/DATASET

