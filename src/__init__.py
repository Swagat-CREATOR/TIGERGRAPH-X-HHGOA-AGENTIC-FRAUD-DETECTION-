"""Agentic Fraud Investigation on TigerGraph (HHGOA).

Shared library for the three pipelines (P1 Plain RAG, P2 GraphRAG,
P3 Agentic GraphRAG). Policy and schema live here so there is exactly one
source of truth every pipeline imports.
"""

__version__ = "0.1.0"

